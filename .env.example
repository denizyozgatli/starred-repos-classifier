# GitHub Personal Access Token (classic or fine-grained with read access to user stars)
# Required ONLY by the data fetch pipeline (never exposed to frontend/browser)
GITHUB_TOKEN=your_github_token_here

# Optional: Gemini API key for fallback classification of ambiguous repositories
GEMINI_API_KEY=your_gemini_api_key_here
